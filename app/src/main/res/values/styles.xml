<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <!-- Widget Styles -->
    <style name="DialogTheme" parent="Theme.RandomContact">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="Theme.RandomContact.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="randomContactMaterialCardViewStyle" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">@color/white</item>
    </style>

    <style name="outlineButtonStyle" parent="Widget.MaterialComponents.Button.OutlinedButton">

    </style>

    <style name="Theme.RandomContact.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.RandomContact.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>